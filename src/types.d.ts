export interface User {
  name: string;
  email: string;
  active: boolean;
  selectUser: string;
}

export interface UserClone {
  id: string;
  name: string;
  email: string;
  active: boolean;
  selectUser: string;
}